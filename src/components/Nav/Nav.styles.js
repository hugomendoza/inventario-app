import styled from "styled-components";
import tw from 'tailwind.macro';

export const StyledNav = styled.nav.attrs({
  className: 'StyledNav',
})`
  ${tw` bg-terciary-600 p-4 flex items-center justify-end`}
`
